<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gerardogarcias.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VIGIA"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/AppTheme.NoActionBar"
            ></activity>
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/home_item">
        </activity>
        <activity
            android:name=".SolicitudesActivity"
            android:label="@string/solicitudes_item">
        </activity>
        <activity
            android:name=".ReportesActivity"
            android:label="@string/reportes_item">

        </activity>
        <activity
            android:name=".IncidenciasActivity"
            android:label="@string/incidencias_item">

        </activity>
        <activity android:name=".prueba"></activity>
    </application>

</manifest>